APP_ENV=local
HOST=0.0.0.0
PORT=8000
CORS_ORIGINS=http://localhost:5173
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_TIMEZONE=
DB_OPTIONS=
# Optional override (leave empty to build from values above)
DATABASE_URL=sqlite:///instance/app.sqlite

# PostgreSQL Configuration (for future chatbot features)
# DATABASE_URL=postgresql://user:password@localhost:5432/chatbot_db
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=chatbot_db
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_password

# Webhook URLs
WEBHOOK_UPLOAD_URL=https://iconic-host.lapage.vn/webhook/demo
WEBHOOK_CHUNKING_URL=https://iconic-host.lapage.vn/webhook/chunking
